[build-system]
requires = [
    "setuptools>=42",
    "wheel",
    "pybind11",
]

build-backend = "setuptools.build_meta"

[tool.cibuildwheel]
test-command = "pytest {project}/tests"
test-extras=["test"]

[tool.cibuildwheel.macos]
archs = ["x86_64", "arm64"]
test-skip = ["*arm64"]
